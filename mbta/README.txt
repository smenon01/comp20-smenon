This lab is part 3 of the mbta lab. The map should now connect to the server created via heroku which serves the JSON real-time data feed of the mbta red line trains. The cross-origin sharing should be enabled on GET /redline.json. This application also attempts to record the latitude/longitude of users accessing the map using HTTP POST—I was able to accomplish this when testing locally, but when I deployed, I was not able to get the location data into the database.

Approximate time spent: 8 hours